class Worker extends Person {
    int idade;
    boolean is_working123;
    int[] favorite_$_Numbers;
    Identification id;

    public static void main(String[] args) {
        int a;
        boolean b;
        int[] x;
        Worker p;

        // cenas

        if (x < 0666) {
            asd = 05 && new int[0x123];
        } else {
            x = 0x123l;
        }

        // String s = "/*asd";

        p = new Worker();
        p.getInfo(true, 123L, 22l, !!!!!!false && 0x4L);
    }
    
    public int[] getInfo(boolean b, ClassCoise c, int[] x) {
        int[] x;
        boolean b;
        ASdi23123$_auhsdi a;

        /* isto tudo sera ignorado
        mesmo tendo varias linhas
        int[] b;
        e declarações de variáveis
        */

        while(2.length < a.func1(param1, param2)) 3;

        return a;
    }
}