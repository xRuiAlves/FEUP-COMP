class Test  {
    public static void main(String[] args) {
        int teste;
        boolean b;
        Test x;

        x = new Test();

        b = x.getBoolean(1);
        b = x.getBoolean();
        // b = x.getBoolean(false);
        // teste = x.getBoolean(1);

        // 2 + 3 * x.getBoolean();
        3 * 5 + x.getInfo(2);
        // 2 + x.getInfo(3) + new X().getInfo(true, new ClassCoise(), new int[4])[2];
        2 + x.getInfo(3) + new X().getInfo(2);

        x = new Test();
        x.getInfo(4) + 2;
        // x.getBoolean(true, true, 1, new int[4], x, new Printer());

        teste = Scanner.readline().length;
        b = Scanner.readline().isStopped();

    }

    public int getInfo(int var) {
        int b2;
        int[] a;
        a = new int[2];
        b2 = 3;

        return a[5];
    }
    
    public int[] getInfo(boolean b, ClassCoise c, int[] x) {
        return new int[5];
    }

    public boolean getBoolean(int a) {
        return false;
    }

    public boolean getBoolean() {
        return true;
    }
}