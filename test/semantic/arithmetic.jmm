class X {
    public static void main(String[] args) {
        int x;
        int[] arr;
        boolean b;

        arr = new int[2];
        x = arr[1];
        b = true && false;

        1 + 2;
        4 - 9;
        1 / 2;
        2 * 3;

        // true + 1;
        // 8 * false;

        x + 1;
        // x * true;

        // x + b;
        2 * 2 + 2 * x;
        // 2 * b + 1 / x;
        2 + arr[1] * x / arr[0];
    }
}