class Worker extends Person {
    int idade;
    Identification id;

    public static void main(String[] args) {
        Worker p;
        int a;
        int[] arr;

        arr = new int[2];
        a = 7;
        p = new Worker();

        arr[2];

        // arr[true];

        2 + 3 * 4;

        p = new Worker();
        // p.getInfo(true);

        // 1.length;
        // 2.length;
        // p.length;
        // args.length;
        // new X().length;

        a = arr.length;
        // a = this.getNonArray().length;
        a = this.getArray().length;
        // a = arr.length.length;

    }

    public int[] getArray() {
        return new int[2];
    } 

    public int getNonArray() {
        return 2;
    }


}