class X {
    public static void main(String[] args) {
        int a;
        int[] arr;
        boolean b;
     
        (1 < 3) && (1 + 2) < (3 * 5);
        1 < 3 && 1 + 2 < 3 * 5;
        b && 1 + 2 < a * 5;

        a < arr[2];

        // a < b;
        // a && b;
        // b < a;
        // !a;
        // ! arr;
        // ! arr[0];
        // arr[0] && a;
    }
}