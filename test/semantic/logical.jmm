class X {
    public static void main(String[] args) {
        int a;
        boolean b;
     
        (1 < 3) && (1 + 2) < (3 * 5);
        1 < 3 && 1 + 2 < 3 * 5;
        b && 1 + 2 < a * 5;

        // a < b;
        // a && b;
        // b < a;
        // !a;
    }
}